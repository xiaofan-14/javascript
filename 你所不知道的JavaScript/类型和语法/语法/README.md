# 语句和表达式

JavaScript中语句相当于句子，表达式相当于短语，运算符则相当于标点符号和连接词。

在 JavaScript 中语句都有一个默认的结果值，可以在控制台中输入语句查看，默认 `undefined`

JavaScript中没有 `else if`， `else if` 极为常见，能省掉一层代码缩进，所以很受青睐。
但这只是我们自己发明的用法，切勿想当然地认为这些都属于JavaScript语法的范畴

JavaScript中的 `&&` 和 `||` 运算符返回它们其中一个操作数的值